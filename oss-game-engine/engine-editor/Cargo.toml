[package]
name = "engine-editor"
version = "0.1.0"
edition = "2021"

[dependencies]
engine-core = { path = "../engine-core" }
engine-renderer = { path = "../engine-renderer" }
engine-assets = { path = "../engine-assets" }
