[package]
name = "engine-build"
version = "0.1.0"
edition = "2021"

[dependencies]
engine-core = { path = "../engine-core" }
engine-assets = { path = "../engine-assets" }
engine-scripting = { path = "../engine-scripting" }
