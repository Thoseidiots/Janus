[package]
name = "engine-assets"
version = "0.1.0"
edition = "2021"

[dependencies]
engine-core = { path = "../engine-core" }
engine-audio = { path = "../engine-audio" }
engine-scripting = { path = "../engine-scripting" }
